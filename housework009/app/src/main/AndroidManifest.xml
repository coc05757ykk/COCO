<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.housework009">
    <!-- 震动 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="18990071-杨可可"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TabButtonActivity" android:exported="true"></activity>
        <activity android:name=".ScheduleDetailActivity" android:exported="true"></activity>
        <receiver android:name=".ScheduleDetailActivity$AlarmReceiver" android:exported="true">
            <intent-filter>
                <action android:name="com.example.housework009.ScheduleDetailActivity.AlarmReceiver" />
            </intent-filter>
        </receiver>
        <activity android:name=".ScheduleActivity" android:exported="true"/>
    </application>

    <uses-permission android:name="android.permission.VIBRATE" />

</manifest>